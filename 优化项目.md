盖了

# LegalMind 项目优化方案

> 基于代码通读后的系统性分析。已完成的标注 ✅，未做的保留待办。
> 最近更新：统一异常处理 + 错误码（RFC 9457 problem+json + traceId 中间件 + SSE 错误通道）、多轮信息收集 HITL 节点（info_gathering 自循环 + LLM 充分性判断）、BM25 索引持久化（pickle + build_index 同步失效）、RAG 检索优化（Reranker/RRF/分块策略/filters 过滤修复）、RAGAS 评估集成、法条补充均已完成。

---

## 一、Bug 修复与基础加固

### 1.1 ✅ workflow.py 键名修复 `message` → `messages`

AgentState 定义了 `messages`（带 `add_messages` reducer），但初始状态传入的是 `"message"`（单数），导致 HumanMessage 丢失。已修复。

### 1.2 ✅ model_client.py 读取 LLM_API_BASE 配置

原来 DeepSeek 分支硬编码 `https://api.deepseek.com/v1`，忽略了 `.env` 中的 `LLM_API_BASE`。已在 Settings 中增加 `LLM_API_BASE` 字段，model_client.py 改为读取该配置。

### 1.3 ✅ LangSmith 配置命名统一

`.env.example` 用 `LANGSMITH_*` 前缀，`config.py` 原来混用了 `LANGCHAIN_*`，pydantic-settings 匹配不上导致追踪不生效。已统一为 `LANGSMITH_*`，并在 `main.py` 中注入 `os.environ` 让 LangChain 自动追踪生效。

### 1.4 ✅ DeepSeek Structured Output 兼容

`with_structured_output` 默认的 `json_schema` response format 在 DeepSeek API 上返回 400 错误。已改用 `method="function_calling"` 模式。

### 1.5 ✅ 前端流式输出自动滚动

原来 watch 只监听 `messages.length`（消息条数），流式输出时条数不变不触发滚动。改为同时监听最后一条消息的 `content.length`。

### 1.6 ✅ Windows 事件循环修复

`main.py` 中修复了 uvicorn 0.49.0 + psycopg async 在 Windows 上的事件循环兼容问题。

---

## 二、Human-in-the-Loop 人机交互 ✅

> 项目的核心亮点之一，展示对 LangGraph interrupt/resume 机制的理解。

### 2.1 三个 HITL 节点

- **check_intent**（意图确认，主图）：在 `intent_recognition` 之后，置信度 < 0.8 时触发 interrupt，生成澄清问题等待用户确认
- **info_gathering**（多轮信息收集，主图自循环）：在 `check_intent` 之后，LLM 判断信息不足时触发 interrupt，追问后自循环回自身，直到信息充分或达到 3 轮上限
- **evaluate**（检索质量，retrieval 子图内）：在 ReAct 检索子图的 `evaluate_node` 中，3 轮自动重试后结果仍 < 3 条时触发 interrupt，引导用户补充信息

### 2.2 相关文件

- `backend/app/agents/human_loop.py` — check_intent（意图确认）+ info_gathering（多轮信息收集自循环）
- `backend/app/agents/retrieval_agent.py` — evaluate_node 内含检索质量 HITL（interrupt）
- `backend/app/llm/prompts.py` — INFO_GATHERING_PROMPT（充分性判断 + 追问生成）
- `frontend/src/components/chat/InterruptCard.vue` — interrupt 交互卡片，支持快捷选项和文本输入

### 2.3 改造文件

- `workflow.py` — State 新增 `intent_confidence`、`user_supplement`、`clarify_round`、`info_sufficient` 字段，图结构插入三个 HITL 节点 + info_gathering 自循环边，新增 `astream_resume()` 方法
- `chat.py` — stream 端点增加 interrupt 检测（通过 `aget_state` 检查 `state.next`），新增 `POST /api/chat/resume` 端点，首个 SSE 事件发送 `session_id`
- `frontend/src/api/chat.js` — 新增 `streamResumeMessage()` 调用 resume 端点
- `frontend/src/stores/chat.js` — 新增 `pendingInterrupt` 状态和 `resumeInterrupt()` 方法
- `frontend/src/views/Chat.vue` — 引入 InterruptCard 组件，interrupt 时替换输入区

### 2.4 info_gathering 自循环机制

```
check_intent → info_gathering ──→ router → ...
                  ↑_____loop_____|
```

- LLM 一次调用完成两件事：`InfoCheckResult(sufficient: bool, question: str)`
- 追问 + 用户回答都追加到 messages（完整 Q&A 上下文）
- `MAX_CLARIFY_ROUNDS=3` 防死循环
- 前端 InterruptCard 复用，无需改动

### 2.5 完整交互时序

```
用户发消息 → /stream → 图执行 → interrupt 打断 → 前端收到 {interrupt} 事件
→ 渲染交互卡片 → 用户回答 → /resume → 图恢复执行 → 可能再次 interrupt 或正常输出
```

---

## 三、Structured Output 意图识别 ✅

### 3.1 IntentAgent 改造

从 `StrOutputParser` 字符串解析改为 `with_structured_output(IntentResult, method="function_calling")`，返回结构化 `IntentResult`：

- `intent`：Literal["qa", "document", "search"]
- `confidence`：float (0-1)
- `reasoning`：str

### 3.2 收益

- 解析成功率 100%（LLM 被约束只能返回合法 Schema）
- `confidence` 驱动 HITL 意图确认节点
- `reasoning` 方便日志追溯

---

## 四、LangSmith 全链路追踪 ✅

### 4.1 集成方式

- `config.py` 中定义 `LANGSMITH_TRACING`、`LANGSMITH_ENDPOINT`、`LANGSMITH_API_KEY`、`LANGSMITH_PROJECT`
- `main.py` 启动时将配置注入 `os.environ`，激活 LangChain 自动追踪
- `.env.example` 提供配置模板

### 4.2 覆盖范围

意图识别 → HITL 确认 → ReAct 检索子图（agent→tools→evaluate→finish） → QA 生成 / 文书生成，全链路可追溯。

---

## 五、法律数据集与评估体系 ✅

### 5.1 数据集（v2）

- `backend/data/legal_eval_dataset_v2.json` — 100 条标注样本，覆盖劳动争议、合同纠纷、婚姻家庭、知识产权、刑事等多领域
- 每条样本包含：question、ground_truth（含引用法条）、case_type 分类

### 5.2 法条补充脚本

- `backend/scripts/supplement_laws.py` — 增量补充缺失法条到 PostgreSQL + JSON
- 已补充 17+ 条文（民法典婚姻家庭编、合同编、侵权责任编、劳动合同法、消保法、道交法、继承法、著作权法、专利法、税收征管法、刑法）
- 增量机制：`check` 字段防止重复插入

### 5.3 RAGAS 评估框架集成

- `backend/scripts/evaluate.py` 支持 `--ragas` 模式
- RAGAS 0.4.3 兼容性处理：ChatVertexAI stub、`reference` 列名、ThreadPoolExecutor 隔离 asyncio、LangchainEmbeddingsWrapper 传 HuggingFace 模型
- 4 个指标：faithfulness、answer_relevancy、context_precision、context_recall
- 当前 5 样本结果：context_recall **0.90**，answer_relevancy **0.95**

### 5.4 自定义 LLM Judge

- 4 维度评估：忠实度、答案相关性、答案正确性、上下文相关性
- 评分尺度：0/0.3/0.5/0.7/1.0
- 当前 5 样本结果：忠实度 **0.82**，答案相关性 **0.94**，答案正确性 **0.82**，上下文相关性 **0.54**

---

## 六、RAG 检索优化 ✅

### 6.1 BGE-Reranker 替换关键词计数 ✅

- `backend/app/rag/reranker.py` 使用 `bge-reranker-v2-m3` CrossEncoder
- ni'yniy条占满 top_k

### 6.2 RRF 融合替代"向量优先"策略 ✅

- `backend/app/rag/retriever.py` 实现 Reciprocal Rank Fusion
- `score(d) = Σ 1/(k + rank_i(d))`，k=60
- BM25 k 对齐向量检索（k=TOP_K*2），提升召回覆盖

### 6.3 分块策略优化 ✅

- 法律文本：1000 字 chunk（覆盖 2-3 个相关条文），overlap=100
- 案例文本：500 字 chunk，overlap=50
- 法条 keywords 拼入 page_content，提升 BM25 命中率
- 对比实验：500字→context_recall 0.67，按条文→0.17（太碎），1000字→**0.90**

### 6.4 ChromaDB 自动清空 ✅

- `backend/scripts/build_index.py` 写入前自动清空旧索引
- `collection.delete(ids=collection.get()["ids"])` 防止新旧数据污染

### 6.5 filters 过滤参数生效修复 ✅

`HybridRetriever.retrieve()` 原接收 `filters` 参数但完全未使用，导致 `search_by_article`、`search_cases` Tool、API 路由传入的过滤条件全部被忽略。

- `_matches_filter()` — 支持 `$contains`/`$in`/`$neq`/简单等值 4 种过滤操作符
- `_is_simple_filter()` — 简单等值过滤下推到 Chroma `similarity_search(filter=...)`，复杂过滤走后置兜底
- RRF 融合后统一后置过滤，确保 BM25 和向量结果都被正确过滤

### 6.6 BM25 索引持久化 ✅

`_init_bm25_from_store()` 原每次启动从 Chroma 全量加载文档重建 BM25 索引。已改为 pickle 持久化：

- 首次启动：从向量库重建 → pickle 持久化到 `./data/bm25_index.pkl`
- 后续启动：直接加载 pickle，跳过向量库查询和索引构建（启动快 10-100x）
- `build_index.py` 重建向量库时自动删除旧 pickle，强制 BM25 同步重建
- 加载/持久化失败不影响运行，回退到重建路径

### 6.7 Embedding 模型升级 【待做】

`bge-small-zh`（512 维）可升级为 `bge-base-zh-v1.5`（768 维），换模型后需重建向量索引。

---

## 七、Tool Calling + ReAct 检索子图 ✅

### 7.1 新增文件

- `backend/app/tools/search_tool.py` — search_cases Tool，4 层错误处理架构
- `backend/app/tools/__init__.py` — 工具包初始化

### 7.2 search_cases Tool

- **参数抽取**：LLM 从自然语言中提取 `court`、`year`、`category` 等结构化参数
- **参数校验**：`CaseSearchInput` Pydantic schema + `field_validator`（年份范围、案由枚举）
- **结构化过滤**：metadata filter 缩小检索范围，提升精度
- **4 层错误处理**：Pydantic 校验 → tenacity 重试（ConnectionError/TimeoutError） → try-except 兜底 → ToolNode `handle_tool_errors=True`

### 7.3 ReAct 检索子图（替代原 case_retrieval 节点）

自行封装的 4 节点子图（未用 `create_react_agent`，保留评估和 interrupt 控制权）：

- **agent**：LLM bind_tools，分析 query 决定 search_cases 调用参数
- **tools**：`ToolNode(_TOOLS, handle_tool_errors=True)` 执行工具调用
- **evaluate**：评估检索结果，≥3 条→done，<3 且 retry<3→自动重试，retry 耗尽→`interrupt()` 人工介入（HITL #2）
- **finish**：整理最终结果返回主图

流程：`START → agent → (有 tool_calls → tools → evaluate → retry|done) | (无 tool_calls → finish) → END`

### 7.4 改造文件

- `workflow.py` — `case_retrieval` + `check_retrieval` 两节点替换为单个 `retrieval_agent` 子图节点，路由简化
- `human_loop.py` — `check_retrieval` 已移入子图 `evaluate_node`，只保留 `check_intent`

---

## 八、后端高性能与高并发（待做）

> 通用概念详见 UPGRADE_PLAN.md 第九节

### 8.1 多级缓存体系 【P0】

Embedding 缓存 + 语义缓存（相似问题直接返回）+ 检索结果缓存。Redis 做 L2，TTL 1h。缓存命中可降本 90%、响应秒级→毫秒级。

### 8.2 限流与熔断 【P1】

Token Bucket 限流保护 LLM API，熔断三态（CLOSED→OPEN→HALF-OPEN）保护下游。Redis sliding-window 或 `pybreaker`。

### 8.3 异步任务队列 【P2】

文档上传/索引构建走 Redis Stream + 后台 Worker，不阻塞查询 API。

### 8.4 连接池调优 【P3】

PostgreSQL: `pool_size=20, max_overflow=10, pool_pre_ping=True, pool_recycle=1800`。考虑 asyncpg 替代 psycopg_async（QPS 2x）。

### 8.5 优雅关闭 + 健康检查 【P0】✅（/ready 待做）

FastAPI lifespan 实现 graceful shutdown（DB/checkpointer 连接的建立与关闭）；`/health`（Liveness）已上线；`/ready`（Readiness，检查 DB/向量库/LLM API）待做。

### 8.6 uvloop 事件循环加速 【P3】

Linux 部署启用 uvloop，QPS +82%，P99 -45%。Windows 不支持。

---

## 九、Agent 架构通用模式（待做）

> 通用概念详见 UPGRADE_PLAN.md 第十节

### 9.1 Map-Reduce 并行检索 【P1】✅

BM25 + 向量检索并行执行，总耗时 = max 而非 sum。已落地 `asyncio.gather` 方案：

- `HybridRetriever.retrieve()`：向量检索（asyncio 原生）与 BM25（同步计算扔 `asyncio.to_thread`）gather 并行，实测单次检索 ~15ms
- Chroma 简单过滤下推改用 `asimilarity_search`（内部 run_in_executor），同步调用不再阻塞事件循环
- 选型说明：固定两路用 gather 即可；LangGraph Send API 留给运行时才能确定路数的动态 fan-out（多专业专家分发，见 15.3）

**顺带修复的 3 个既有性能 bug（API 层验证时暴露）：**

1. **请求级实例化 Reranker（最严重）**：`RetrievalAgent()` 在 cases/documents/search_tool 5 处调用点每次请求 new → 每次重新加载 568M cross-encoder 模型。已加 `get_retrieval_agent()` 进程级单例
2. **Reranker 同步阻塞**：`CrossEncoder.predict()` 在 async 函数里直接调用，推理期间整个事件循环停摆 → 改 `asyncio.to_thread`
3. **rerank 全长推理**：bge-reranker-v2-m3 默认 8192 上下文，1000 字 chunk 全长推理 ~20s → `max_length=256` + 文档前缀截断 400 字符，降至 ~9.3s（568M 模型 CPU batch 推理的物理极限，进一步优化需换小模型/GPU/ONNX 量化）

实测：`GET /api/cases/search` 稳态 20s+ → 9.3s；其中检索（gather 并行）仅 ~15ms，剩余全是 rerank 推理

### 9.2 Self-Reflection 质量评估节点 【P2】

QA 生成后 LLM 自评忠实度，低分触发重新检索/生成。高风险场景（法律）必备。

### 9.3 背压控制 【P2】

`asyncio.Semaphore` 限制并发 LLM 调用数，防止高并发打挂 API。

### 9.4 上下文窗口管理 【P3】✅（方案升级为"视图裁剪 + 自动摘要压缩"，见 10.5）

`RemoveMessage` 裁剪消息历史，保留系统消息 + 最近 N 轮。防止 context window 爆炸。

### 9.5 智能路由 【P3】

简单问候不检索直接回复；事实型走 RAG；文书生成走专用流程。可减少 30-50% 无谓调用。

---

## 十、LangGraph/LangChain 高阶用法（待做）

> 通用概念详见 UPGRADE_PLAN.md 第十一节

### 10.1 astream_events 分阶段进度推送 【P1】✅

已落地：`stream_mode=["messages", "updates"]` 多路复用（[workflow.py](backend/app/agents/workflow.py)），前端实时展示"正在理解您的问题..."→"识别为：法律咨询"→"正在检索相关案例..."→"找到 N 条相关案例"→"正在生成回答..."。

**实现要点（面试可讲）：**

1. **多路复用而非 astream_events**：`stream_mode` 传列表同时订阅两个通道——messages 出逐 token（打字机效果不变），updates 出节点完成增量（派生阶段事件）。相比 `astream_events` v2：事件量少一个量级、无需过滤海量内部事件、interrupt 检测逻辑（`_check_interrupt`）零改动
2. **updates 派生而非存储阶段**：阶段事件从节点输出现场推导——意图名取 `output.intent`、案例数取 `len(output.retrieved_cases)`，不新增 state 字段；`__interrupt__` 键跳过（HITL 由 API 层统一处理）
3. **归一化事件流**：workflow 层 yield `{"type": "token"|"stage", ...}`，API 层只做 SSE 转发，前端 `stage` 事件渲染进度行（running 转圈 / done 打勾），正文开始输出后自动隐藏
4. **resume 不发意图阶段**：`astream_resume` 从 checkpoint 断点继续，父图节点不重跑，阶段事件天然只从断点后开始推

### 10.2 PostgresSaver 持久化 Checkpoint 【P0】✅

替代 `MemorySaver`，Agent 状态持久化到 PostgreSQL，重启不丢、HITL 中断可恢复。

### 10.3 子图状态隔离 【P3】

`StateGraph(State, input=Input, output=Output)` 明确子图 I/O Schema，隐藏内部字段。

### 10.4 with_fallbacks 容灾链 【P3】✅

已落地 `main.with_fallbacks([backup], exceptions_to_handle=...)`（[model_client.py](backend/app/llm/model_client.py)），主/备同为 DeepSeek 独立实例（不引入新模型），6 类网络/限流异常显式配全。对 `bind_tools` / `with_structured_output` 透明（fallback 均为 ChatModel，方法代理可用）。

**关键实现决策（面试可讲）：**

1. **异常名单必须显式配全**：`openai.APIConnectionError` 不是内置 `ConnectionError` 的子类，漏配 = fallback 永不触发（静默失效，验证脚本实测抓到）
2. **规则兜底不上链尾**：链尾 Lambda 会破坏 `with_structured_output` 属性代理（Lambda 无同名方法，服务器启动即崩）；规则兜底由应用层承担 — intent 失败降级 HITL、SSE error 事件等，带上下文且类型安全
3. **流式必须 astream**：`RunnableWithFallbacks.stream()` 是同步 generator 无法 `async for`；"第一个 chunk 前"失败自动切换备实例，中途失败不重试（避免已输出 token 重复），走 SSE error 事件

不只 LLM，检索器/解析器也能 fallback（同一模式）。

### 10.5 Context 管理 — 视图裁剪 + 自动摘要压缩 【P3】✅

**方案选型（为何不是 Custom Reducer 物理裁剪）**：物理删除 checkpoint 里的 messages 会破坏 HITL resume（interrupt 恢复依赖完整 state），且"纯裁剪"会丢法律场景的关键事实（金额/期限/当事人）。最终采用**三层防线**：

1. **视图层预算裁剪**（[context_manager.py](backend/app/llm/context_manager.py) `split_history`）：checkpoint 全量保留，只在构造 LLM 输入时按字符预算（`HISTORY_CHAR_BUDGET=6000` ≈ 3-4k token）从最新往回保留完整轮次（轮次边界 = HumanMessage），兜底保证当前问题永不丢
2. **增量摘要压缩**：落入裁剪区且未摘要的内容超过 `SUMMARY_TRIGGER_CHARS=2000` 才触发一次 LLM 压缩（结构化事实提取 prompt），摘要与已有摘要合并后存入 `state.context_summary`，`summarized_count` 游标记录已覆盖前缀，下轮只增量压缩新落入部分；注入位置：system 之后、原文之前
3. **手动逃生舱**：前端"新对话"按钮即手动重置（已存在）

**接入点**：`_qa_node`（qa 的 messages 视图 + 摘要）、`info_gathering`（充分性判断的 history 文本，[human_loop.py](backend/app/agents/human_loop.py) `format_history_text`）。

**通用价值（面试可讲）**：

1. **视图 vs 物理**：数据所有权（checkpoint）与使用视图（prompt）分离——同一模式见于 MVCC 快照读、COW
2. **分层防御**：预算内不动作（零成本），超预算先压缩再裁剪（保事实），极端情况兜底保当前问题（可用性优先）
3. **预算单位用字符而非 token**：中文 1 字 ≈ 0.6 token，字符预算是保守上界，零 tokenizer 依赖

**E2E 验证**：32 条消息 16041 字（超预算 2.6 倍）→ `summarized_count=26`，摘要保留全部关键事实（月薪 15000/2N=60000/法条 87·47·82 条/时效）；3 轮对话 + HITL 自动 resume 全链路无回归（正文 12679 字）。

### 10.6 Token 成本追踪（Custom Callbacks）【P3】✅

`BaseCallbackHandler.on_llm_end` 提取 `usage_metadata` → 带 traceId 的结构化日志（[usage_tracker.py](backend/app/llm/usage_tracker.py)），含模型名、in/out/total tokens、成本估算（元/百万 token 单价表）、延迟。挂载在模型构造参数 `callbacks=[...]`（实例保持 BaseChatModel，方法代理零影响）。

**两个实现要点（面试可讲）：**

1. **ContextVar 桥接 traceId**：纯 ASGI 中间件写的 `scope.state` 在 langchain 回调里不可见；`ContextVar` 在同一 asyncio 调用链内自动传播，中间件 `set` → 回调 `get`，日志与请求通过 traceId 关联
2. **模型名从响应取**：新版 langchain 的 `serialized` 不再带 `kwargs.model`，改从 `llm_output.model_name` / `response_metadata` 提取；字段优先 `usage_metadata`（跨模型标准），DeepSeek 的 `llm_output.token_usage` 兜底

**全链路用量呈现（已落地）：**

- **请求级累计**：`RequestUsage` 累计器（input/output/calls），`TraceIdMiddleware` 每请求 `usage_var.set(RequestUsage())`，一次请求内所有 LLM 调用（意图/澄清/问答/元数据）在 `on_llm_end` 自动归集
- **SSE `usage` 事件**：流结束时发 `{input_tokens, output_tokens, total_tokens, calls}`；HITL interrupt 打断的对话同样照发（token 已消耗）
- **持久化**：`chat_messages` 新增 `usage JSONB` 列（迁移 `0003_add_message_usage`），随 assistant 消息落库，历史会话加载不丢失
- **前端展示**：消息气泡下方显示"消耗 N tokens（输入 X / 输出 Y · Z 次调用）"
- **E2E 验证**：SSE 事件 / DB 落库 / API 读取三处数据一致（E2E_OK）

配额/计费 API 与用量统计页仍为后续可扩展层（采集格式已按归因设计：trace_id + model + scene 可加）。

---

## 十一、RAG 管道深度优化（待做）

> 通用概念详见 UPGRADE_PLAN.md 第十二节

### 11.1 上下文压缩 【P1】

送入 LLM 前压缩检索内容（摘要/关键句提取）。Meta REFRAG 实测 TTFT 加速 30x。

### 11.2 块顺序优化（Lost in the Middle）【P3】

高置信度文档放上下文首尾，辅助内容放中间。零成本提升质量。

### 11.3 批量 Embedding 【P3】

批量 embedding 比单条快 5-10x，向量库批量插入快 10-50x。

### 11.4 动态检索策略 【P3】

简单问题 → 小 top_k（5）；复杂问题 → 大 top_k（20）+ Reranker 精排。

---

## 十二、生产工程化（待做）

> 通用概念详见 UPGRADE_PLAN.md 第十三节

### 12.1 OpenTelemetry 全链路追踪 【P2】

一个 trace_id 贯穿 HTTP→DB→LLM→向量检索全链路。比 LangSmith 更通用（不绑定 LangChain）。

### 12.2 structlog 结构化日志 【P3】

JSON 格式日志，非阻塞写入，支持 ELK/Loki 解析。

### 12.3 Docker 多阶段构建 【P3】

Builder 阶段编译，Runtime 阶段精简，镜像体积减 60-80%。

### 12.4 CI/CD 流水线 【P3】

GitHub Actions: `lint → test → build → deploy`，PR 合并自动部署。

### 12.5 安全加固 【P3】

JWT 密钥用 `secrets.token_urlsafe(32)`，密码哈希升级 bcrypt，API 限流，CORS 白名单。

### 12.6 pytest 重写测试 【P3】

当前 `test/` 是脚本文件，改为 pytest + pytest-asyncio，带断言和 fixture。

### 12.7 前端引用来源展示 【P3】

后端已返回 `sources`，前端 `ChatMessage.vue` 补充引用卡片渲染。

### 12.8 清理未使用依赖 【P3】

Redis 在 docker-compose 和 pyproject.toml 中声明但代码中未使用，删除或真正用起来（缓存/限流）。

### 12.9 ✅ 统一异常处理 + 错误码（RFC 9457）【P3】

- 新增 `backend/app/exceptions/`（codes 错误码注册表 / base 异常基类 / handlers 全局处理器 + TraceId 纯 ASGI 中间件），`main.py` 两行注册
- 所有错误响应统一为 `application/problem+json`：标准字段（type/title/status/detail/instance）+ 扩展字段（code/traceId），参数校验 422 → 400
- 替换全部 9 处裸 `HTTPException`（auth/dependencies/chat/cases），新增 RAG_001 检索集成错误包装（`cases.py` /search）
- 修复 SSE 流信息泄漏：原来 `chat.py`/`documents.py` 把原始异常 `{e}` 吐给用户（可能含连接串/模型路径），改为流内 `error` 错误事件（code + detail + traceId），堆栈只进日志
- 前端适配：`client.js` 拦截器解析 problem 体（`app_code`/`app_message`/`trace_id`），`chat.js`/`documents.js` 流式错误展示后端文案
- TraceId 用纯 ASGI 中间件而非 BaseHTTPMiddleware（后者破坏 SSE 客户端断连检测），请求日志格式 `[traceId] METHOD path -> status (ms)`

### 12.10 ✅ Structured Output 生产加固 + LegalAnswerMeta 元数据抽取【P3】

**加固（两处既有结构化输出补齐容错）：**

- `.with_retry(stop_after_attempt=2)`：网络抖动/格式不合规等瞬时失败重试一次即恢复
- **意图识别降级**：失败返回低置信度默认值 qa（0.55 < 0.8）→ 自动触发 HITL #1 澄清，让用户口头确认——结构化输出失败被"人机协作"兜住，非关键路径失败不阻塞主流程
- **信息充分性降级**：判断失败直接放行（宁可少追问一轮，不能不让用户拿答案）；同时防御 function_calling 模式下模型拒答返回 `None` 而非抛异常的坑
- `InfoCheckResult.question` 改 `Field(default="")`：充分时模型不必被迫编一个追问

**LegalAnswerMeta（QA 主链路，"流式 + 元数据抽取"方案）：**

- **设计权衡**：`with_structured_output` 与逐 token 流式互斥（JSON 必须完整才能反序列化），直接替换 `qa_generation` 会牺牲打字机效果。解法：正文保持自由文本流式，流结束后对完整文本做一次轻量结构化抽取——两全其美
- `QAAgent.extract_meta()`：`LegalAnswerMeta(summary/risk_level/applicable_laws)` schema + `META_EXTRACT_PROMPT`，`_qa_node` 流结束调用，抽取的 1-2s 发生在 token 已送达之后用户无感知；失败返回 None 不阻塞（元数据是增强不是关键路径）
- state 新增 `answer_meta` 字段，`chat.py._finalize_stream` 复用已有的 `aget_state` 零成本送出 `meta` SSE 事件；前端 `_consumeStream` 捕获后由 `ChatMessage.vue` 渲染答案卡片（风险等级配色 低=绿/中=黄/高=红 + 法条标签 + 一句话结论）
- **元数据持久化**：`ChatMessageRecord` 新增 `meta JSONB` 列，`_record_messages` 连同元数据一起写入，`GET /sessions/{id}/messages` 带出——刷新页面/加载历史会话时答案卡片不丢失；前端 `getMessages` 整对象赋值自动携带 meta，无需改动
- **数据库迁移体系（替代手动 ALTER TABLE）**：tortoise-orm ≥ 1.0 内置迁移 CLI（官方对 1.0+ 的推荐，替代 Aerich——查证后确认项目从未装过 Aerich，记忆有误）。`database.py` 重构为标准 `TORTOISE_ORM` config dict（运行时 init 与迁移 CLI 单一配置来源），排障记录：`MigrationLoader` 只认 config 里显式的 `migrations` 键（`makemigrations` 走推断逻辑所以能工作，`heads/history/migrate` 却找不到文件——加 `"migrations": "app.models.migrations"` 解决）
  - 常用命令：`uv run python -m tortoise -c app.db.database.TORTOISE_ORM makemigrations|migrate|history|sqlmigrate`
  - 存量库基线：首次全量迁移用 `migrate --fake` 登记（只记状态不执行 SQL）；此后 `0002_add_message_list_index` 走真实流程验证通过（生成 → sqlmigrate 审查 SQL → 事务内执行 → pg_indexes 验证）
  - 0002 迁移本身也有价值：`chat_messages(chat_session_id, created_at)` 复合索引——历史消息查询 `WHERE chat_session_id=? ORDER BY created_at` 的精确匹配（PostgreSQL FK 列不自动建索引，与 MySQL 不同）
- E2E 实测：`{"summary": "合同期满公司不续签，您有权获3个月工资经济补偿金。", "risk_level": "低", "applicable_laws": ["《劳动合同法》第四十四条", ...]}`，DB 落库与 API 读取均验证通过

**顺带修复既有 bug**：`retrieval_agent.py` 子图 `tools_condition` 映射键写错（`"end"` 应为 `"__end__"`），LLM 不调工具直接回答时 `KeyError: '__end__'` → 500。正常流程从不触发（LLM 总是调 search_cases），多轮 interrupt/resume 后才暴露——正是全局异常兜底最先捕获的 bug

---

## 十三、简历包装（待做）

### 13.1 项目名修正

`RAG_langgrpah-legalMind` → `RAG-langgraph-LegalMind`（修正 langgraph 拼写）

### 13.2 README 完善

添加系统架构图、功能特性列表、快速开始指南、评估指标数据。

### 13.3 STAR 简历描述

参考 UPGRADE_PLAN.md 第四节，按实际实现的功能撰写。

---

## 十四、LangGraph 1.0+ 新特性集成（待做）

> 通用概念详见 UPGRADE_PLAN.md 第十四节

### 14.1 Pre/Post Model Hooks（模型护栏）【P1】

LLM 调用前后自动执行 guardrail 逻辑，拦截敏感输入/输出。

### 14.2 Node Caching（节点缓存）【P2】

意图识别等确定性节点缓存结果，省 LLM 调用。

### 14.3 Cross-Thread Memory（跨会话记忆）【P2】

`Store` API 按 user_id 存储长期记忆，跨会话保留用户画像。

### 14.4 Delta Channels（增量通道）【P2】

Checkpoint 只存增量变化，长对话存储成本降 100x。

### 14.5 Typed Streaming Events（类型化流式事件）【P1】✅

已随 10.1 落地：workflow 层归一化 `{"type": "token"|"stage", ...}` 事件流，SSE 端点按类型转发（见 10.1）。

### 14.6 Programmatic Tool Calling【P3】

LLM 写代码编排工具调用，中间步骤不回传 LLM，省 token。

### 14.7 interrupt_before/after（声明式 HITL）【P3】

编译时声明暂停节点，代码侵入性低于手动 `interrupt()`。

### 14.8 Swarm/Hierarchical 多 Agent 拓扑【P3】

扩展到多专业领域时，用层级或 Swarm 模式协作。

### 14.9 Recursion Limit（图递归深度限制）【P1】

`recursion_limit` 系统级防死循环，生产必备。

### 14.10 Subgraph Streaming（子图事件流）【P2】

`subgraphs=True` 流式获取子图内部节点更新。

---

## 十五、GitHub 热门架构模式与语法实践（待做）

> 通用概念详见 UPGRADE_PLAN.md 第十五节

### 15.1 Agentic RAG：文档评分 + 查询重写【P1】

检索后 LLM 评分文档相关性，不相关则重写 query 重查。

### 15.2 Self-Reflective ReAct + 流式工具校验【P1】

agent 节点校验 tool_calls 合法性 + Pydantic 参数校验，拦截 94% 幻觉调用。

### 15.3 Hierarchical Agent Teams（层级 Agent 团队）【P2】

扩展多专业领域时用 Send API fan-out 到专业子 Agent，>10 工具延迟降 40%。

### 15.4 Multi-Agent Debate（多 Agent 辩论）【P3】

proponent→opponent→judge 三方辩论，事实一致性 +40%。

### 15.5 Send API 并行 Map-Reduce【P1】✅

多路检索（BM25+向量）并行执行，延迟 sum→max。已用 `asyncio.gather` 落地（见 9.1）；Send API 本体保留给 15.3 多专家动态分发场景。

### 15.6 Checkpointer 选型策略【P0】✅

开发用 MemorySaver，生产上 PostgresSaver，跳过 SqliteSaver。

### 15.7 Langfuse 开源可观测性替代【P2】

LangSmith SaaS 合规问题场景用 Langfuse 自部署替代。

### 15.8 Cluster 弹性伸缩架构【P3】

多区域部署 + 状态分区 + K8s Operator 自动扩缩容。

### 15.9 Pydantic v3 状态校验加速【P3】

State Schema 从 TypedDict 升级为 Pydantic BaseModel，获得运行时校验。

### 15.10 Prompt 工程模式库【P3】

prompt 从代码字符串中分离到 `prompts/` 目录集中管理。
